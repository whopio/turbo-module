{
  "name": "@whop/turbo-module-monorepo",
  "version": "0.1.0-canary.4",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {},
  "packageManager": "pnpm@10.23.0"
}
